import numpy as np
import matplotlib.pyplot as plt;plt.ion()

alpha = np.radians(20)

x, y = 0., 0
z0 = 1

norm = np.array([np.cos(alpha), np.sin(alpha), 0])
ss = np.array([x, y, z0])

# plt.plot(0,0,'x')
# plt.plot(x,y,'+')
# plt.plot([0,x], [0,y])


breakpoint()
